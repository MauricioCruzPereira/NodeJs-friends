<%- include("../../partials/head.ejs") %>

    <body>
        <%- include("../../partials/navbarUser.ejs") %>
            <div class="hero is-fullheight has-background-info-light">
                <div class="hero-body">
                    <div class="container">
                        <div class="divSection">
                            <div class="box1">

                                <div class="tabs is-centered is-toggle is-toggle-rounded">
                                    <ul>
                                        <li><a a href="/user/friends">Amigos</a></li>
                                        <li><a href="/user/sent">Pendentes</a></li>
                                        <li class="is-active"><a a href="/user/pending">Solicitações</a></li>
                                    </ul>
                                </div>

                                <h1>Solicitações Recebidas</h1>
                                <% friends.forEach(friend=> { %>
                                    <% users.forEach(user=> { %>
                                        <% if (friend.idRecipient==usuario.id) { %>
                                            <% if (friend.idSender==user.id ) { %>
                                                <div class="list1">
                                                    <div class="imgBx1">
                                                        <img src="/uploads/<%= user.avatar %>" alt="">
                                                    </div>
                                                    <div class="content1">
                                                        <div class="rank1">

                                                            <form action="/user/answer" method="post"
                                                                style="float:left; margin-right: 5px;">
                                                                <input type="hidden" name="id" value="<%= user.id %> ">
                                                                <input type="hidden" name="response" value="accept">
                                                                <input type="submit" value="Aceitar"
                                                                    class="button is-success is-small">
                                                            </form>

                                                            <form action="/user/answer" method="post"
                                                                style="float:left; margin-left: 5px;">
                                                                <input type="hidden" name="id" value="<%= user.id %> ">
                                                                <input type="hidden" name="response" value="decline">
                                                                <input type="submit" value="Recusar"
                                                                    class="button is-danger is-small">
                                                            </form>

                                                        </div>
                                                        <h4>
                                                            <%= user.name %>

                                                        </h4>
                                                        <p>
                                                            <!-- tratamento para deixa letra maiuscula -->
                                                            <%= user.profession!=null || "" ?
                                                                user.profession.substring(0,1).toUpperCase()+user.profession.substring(1)
                                                                : "Nenhum emprego" %>
                                                        </p>
                                                    </div>
                                                </div>
                                                <% } %>
                                                    <% }%>
                                                        <% }) %>

                                                            <% }) %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
    </body>

    <% - include("../../partials/rodape.ejs") %>